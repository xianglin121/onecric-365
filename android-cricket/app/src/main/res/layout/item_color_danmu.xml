<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginRight="12dp">
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/iv_color"
        android:layout_width="32dp"
        android:layout_height="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:src="#1A8FFA"/>

    <TextView
        android:id="@+id/tv_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/iv_color"
        app:layout_constraintBottom_toBottomOf="@id/iv_color"
        app:layout_constraintLeft_toLeftOf="@id/iv_color"
        app:layout_constraintRight_toRightOf="@id/iv_color"
        android:textSize="10dp"
        android:textColor="@color/white"/>

    <ImageView
        android:id="@+id/iv_indicator"
        android:layout_width="6.5dp"
        android:layout_height="2dp"
        app:layout_constraintTop_toBottomOf="@id/iv_color"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="6dp"
        android:background="@drawable/shape_color_danmu_indicator"
        android:visibility="invisible"/>
</androidx.constraintlayout.widget.ConstraintLayout>